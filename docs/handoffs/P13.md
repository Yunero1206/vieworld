# Handoff: P13 — Session Variants (Listening Room & Live House)

## Summary of Accomplishments
- Implemented **P13 — Session Variants (Listening Room & Live House)**, strictly adhering to `docs/CONSTITUTION.md` (§2.3, §4, §7.2), `docs/CONTRACTS.md` (§2, §3.3, §7.2), `docs/packets/P13.md`, and deterministic domain invariants.
- Enforced session contract reuse: Both `listening` and `concert` variants reuse the unified `Session` domain contract and the canonical `/sessions/:sessionId` venue view (`SessionView`), preventing duplicate code and detached applications.
- Recorded architectural decision **DEC-012** in `docs/DECISIONS.md`: Unified Session Contracts for Format Variants, User-Initiated Audio, and Cross-View Expiration Consistency.
- Added format-specific types to `src/domain/types.ts`:
  - `TrackNote` (`trackNumber`, `title`, `duration`, `notes`, `isCurrent`).
  - `SetlistItem` (`order`, `title`, `status`, `notes`).
  - `CallSampleCue` (`id`, `cueText`, `prompt`, `actionLabel`).
  - `mediaStatus` (`'cleared_local' | 'missing' | 'expired'`).
- Enriched canonical domain fixtures in `src/data/fixtures.ts`:
  - `session-listen-01`: `format: 'listening'`, `hostRole: 'team'`, `segmentMode: 'recorded'`, track liner notes, `mediaStatus: 'cleared_local'`.
  - `session-house-01`: `format: 'concert'`, `hostRole: 'artist'`, `segmentMode: 'live'`, setlist, call sample cues, `avatarAssetId: 'avatar-a-v1'`.
  - `session-expired-01`: `format: 'listening'`, `status: 'ended'`, `replayStatus: 'expired'`, `mediaStatus: 'expired'`.
- Enforced user-initiated audio playback (**no autoplay audio** invariant):
  - Initial `isPlayingAudio` state initialized to `false` in `SessionView`, preventing unsolicited autoplay on page entry.
- Built Listening Room features:
  - `TrackNotesPanel` (`src/components/TrackNotesPanel.tsx`) with track numbers, titles, durations, active track indicator, liner notes inspector, and cleared local media disclaimer.
  - Truthful header tags: `host-role-tag` ("Đội ngũ phụ trách (Team)") and `segment-mode-tag` ("Đã ghi hình trước (Recorded)").
- Built Live House features:
  - Larger concert stage in `src/components/AvatarStage.tsx` (`minHeight: 440px`, concert lighting, approved event outfit badge).
  - `SetlistPanel` (`src/components/SetlistPanel.tsx`) with status chips (`performing`, `completed`, `upcoming`) and current song marquee.
  - `CallSampleCueBar` (`src/components/CallSampleCueBar.tsx`) with interactive trigger buttons, feedback messages, and explicit constitutional disclaimer disclaiming multi-user WebRTC audio synchronization or jam claims.
- Handled missing media & expired rights:
  - `SilentMediaPlaceholder` (`src/components/SilentMediaPlaceholder.tsx`) renders informative warning for `mediaStatus: 'missing'` while keeping silent spectrum fully usable.
  - Playback button disabled with lock icon when `mediaStatus: 'expired'`.
  - Replay expiration consistently represented across `SessionView` (`expired-rights-session-banner`), `WorldDetailView` Archive tab (`archive-replay-expired-${s.id}`), and `MyWorldView` Capsules tab (`replay-expired-notice-${capsule.id}`), while preserving private notes and capsules.
- Verified Acceptance Test Suite `src/tests/session_variants.test.tsx` (9/9 passed):
  1. Contract Reuse: Listening Room reuses unified contract and `/sessions/:sessionId`.
  2. Contract Reuse: Live House reuses unified contract and `/sessions/:sessionId`.
  3. User-Initiated Audio: No autoplay audio; requires user interaction to toggle play/pause.
  4. Listening Room: Truthful team-host role, recorded segment badge, and track liner notes.
  5. Live House: Larger stage, setlist with current song, and event outfit badge.
  6. Live House: Call sample cues trigger interaction simulation and display truthful disclaimer.
  7. Missing Media: Gracefully falls back to silent demo mode without breaking audience interactions.
  8. Expiration Consistency: Consistently represents expired rights across SessionView, Archive, and My World.
  9. Constitutional Boundaries: Confirms absence of third-party login prompts or external media ripping claims.
- Total test suite: 146/146 passed across 14 test files.
- TypeScript typecheck passed cleanly (`tsc --noEmit` exit code 0).
- Production build succeeded (`tsc -b && vite build` compiled in 3.61s).
- Updated `docs/TEST_MATRIX.md` marking T13 as `PASSED`.
- Updated `docs/BUILD_STATE.md` setting next action to `P14 — Bounded World Guide (deterministic app assistance)`.

## Verified Evidence
- `npm.cmd run typecheck`: exit code 0 (passed at 2026-09-09T19:09:24+07:00)
- `npm.cmd test`: exit code 0, 146 passed across 14 test files (passed at 2026-09-09T19:09:10+07:00)
- `npm.cmd run build`: exit code 0, compiled successfully in 3.61s (passed at 2026-09-09T19:09:24+07:00)

## Next Resume Action
- Open `docs/packets/P14.md`.
- Implement **P14 — Bounded World Guide (deterministic app assistance)**:
  - Implement bounded deterministic in-app guide component answering membership, benefit, order, and venue questions.
  - Guarantee deterministic answers without external LLM network calls.
  - Decline unknown queries honestly without hallucinating.
  - Protect against prompt injection attempts trying to mutate state or bypass guards.
  - Verify Acceptance T14 in `src/tests/guide.test.tsx`.
