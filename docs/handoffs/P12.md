# Handoff: P12 — Operator Session Console

## Summary of Accomplishments
- Implemented **P12 — Operator Session Console**, strictly adhering to `docs/CONSTITUTION.md` (§2.3, §4, §7.2), `docs/CONTRACTS.md` (§2, §3.3, §7.2), `docs/packets/P12.md`, and deterministic domain invariants.
- Relocated temporary operator controls from the transient scenario drawer to dedicated Studio pages (`/studio/operator` and `/studio/operator/:sessionId`).
- Recorded architectural decision **DEC-011** in `docs/DECISIONS.md`: Operator Session Orchestration, Rights Clearance Guardrails, and Cancellation Immutability.
- Extended domain types and fixtures (`src/domain/types.ts`, `src/data/fixtures.ts`):
  - Added `SessionRightsChecklist`, `rightsApproved`, `rightsChecklist`, and `isChatPaused` to `Session`.
  - Added canonical approved fixtures `session-dropin-01` and `session-listen-01` (`rightsApproved: true`).
  - Added canonical unapproved fixture `session-house-01` (`rightsApproved: false`, checklist false).
  - Added operator actions: `OPEN_LOBBY`, `PAUSE_SESSION`, `RESUME_SESSION`, `CANCEL_SESSION`, `UPDATE_SEGMENT_MODE`, `TOGGLE_CHAT_PAUSED`, `APPROVE_SESSION_RIGHTS`, `DISCONNECT_ARTIST`, `RECONNECT_ARTIST`, `SELECT_QUESTION`, `CLOSE_QUESTION`, `PUBLISH_REPLAY`, `WITHDRAW_REPLAY`.
- Extended domain reducer (`src/domain/reducer.ts`):
  - Pre-broadcast guard hierarchy: avatar validity and retirement evaluated first (`AVATAR_RETIRED`, `AVATAR_NOT_APPROVED`), followed by synthetic rights clearance check (`RIGHTS_NOT_APPROVED`).
  - Broadcast lifecycle state machine: `draft` ⟶ `lobby` ⟶ `live` ⟷ `paused` ⟶ `ended`, plus immutable `cancelled`.
  - Cancellation immutability: `CANCEL_SESSION` transitions status to `cancelled`. `JOIN_SESSION` and `START_SESSION` reject cancelled sessions with `SESSION_CANCELLED`. Page reload preserves cancellation immutability.
  - Real-time moderation: pause/resume session, live/recorded segment switching, chat pausing (`TOGGLE_CHAT_PAUSED`), question selection and dismissal, replay publish and withdraw.
  - Flexible payload compatibility supporting both root action properties and nested payloads.
- Real-time Fan Stage Integration:
  - `src/components/FanChatPanel.tsx`: renders `[data-testid="chat-paused-notice"]` with copy *"Kênh chat đang tạm dừng bởi điều hành viên"* and disables chat input when `isChatPaused: true`.
  - `src/views/SessionView.tsx`: passes `isChatPaused` to fan chat panel; dynamically displays `[data-testid="active-selected-question-banner"]` with copy *"★ CÂU HỎI ĐANG ĐƯỢC NGHỆ SĨ TRẢ LỜI TRỰC TIẾP"* upon operator question selection.
- Built `OperatorConsoleView` (`src/views/OperatorConsoleView.tsx`) mounted at `/studio/operator` and `/studio/operator/:sessionId`:
  - Prominent Local-Only Role Preview disclosure explaining that role switching is a local operational UI test preview, NOT backend-enforced RBAC.
  - Active session switcher with status chips (`draft`, `lobby`, `live`, `paused`, `ended`, `cancelled`).
  - Pre-broadcast Rights Clearance Checklist (music clearance, artist consent, safety review) with simulated approval action.
  - Lifecycle orchestration controls (Open Lobby, Start Broadcast, Pause, Resume, End, and permanent Cancel).
  - Artist presence simulation toggles (Disconnect / Reconnect artist avatar).
  - Segment mode toggle (`live` ⟷ `recorded`).
  - Question moderation desk with Select and Dismiss actions.
  - Fan chat freeze control with live indicator.
  - Replay lifecycle governance (Publish / Withdraw recording).
  - Direct navigation link to live fan stage (`[data-testid="view-fan-stage-link"]`).
- Updated `StudioOverviewView` (`src/views/StudioOverviewView.tsx`) with active link to Operator Console.
- Verified Acceptance Test Suite `src/tests/operator.test.tsx` (14/14 passed):
  1. Blocks `START_SESSION` when rights checklist is unapproved with `RIGHTS_NOT_APPROVED`.
  2. Evaluates avatar retirement check prior to rights clearance (`AVATAR_RETIRED`).
  3. Approving rights checklist allows starting session.
  4. Broadcast lifecycle progression: `draft` → `lobby` → `live` → `paused` → `live` → `ended`.
  5. Cancelled session is permanently immutable and cannot be joined (`SESSION_CANCELLED`).
  6. Operator console renders disclaimer and disallows starting unapproved session.
  7. Approving rights from UI enables start broadcast button.
  8. Broadcast controls (Lobby, Start, Pause, Resume, End) transition session states.
  9. Artist presence disconnect/reconnect immediately updates presence status.
  10. Segment mode toggle switches between `live` and `recorded`.
  11. Question selection sets `selectedQuestionId` and closes question.
  12. Freezing fan chat sets `isChatPaused` and propagates notice/input disable to fan view.
  13. Replay publication and withdrawal transitions replay status between `available` and `withdrawn`.
  14. Session cancellation persists across simulated page reloads and blocks joining.
- Total test suite: 137/137 passed across 13 test files.
- TypeScript typecheck passed cleanly (`tsc --noEmit` exit code 0).
- Production build succeeded (`tsc -b && vite build` compiled in 3.42s).
- Updated `docs/TEST_MATRIX.md` marking T12 as `PASSED`.
- Updated `docs/BUILD_STATE.md` setting next action to `P13 — Session Variants (Listening Room & Live House)`.

## Verified Evidence
- `npm.cmd run typecheck`: exit code 0 (passed at 2026-09-09T18:49:15+07:00)
- `npm.cmd test`: exit code 0, 137 passed across 13 test files (passed at 2026-09-09T18:50:35+07:00)
- `npm.cmd run build`: exit code 0, compiled successfully in 3.42s (passed at 2026-09-09T18:50:48+07:00)

## Next Resume Action
- Open `docs/packets/P13.md`.
- Implement **P13 — Session Variants (Listening Room & Live House)**:
  - Implement format-specific layouts and behaviors for Listening Room (`listening`) and Live House (`concert`).
  - Reuse unified session contracts (`Session`, `SessionView`).
  - Display truthful labels during recorded segments, prevent autoplay audio, enforce rights expiration playback disabling.
  - Verify Acceptance T13 in `src/tests/session_variants.test.tsx`.
