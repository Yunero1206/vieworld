# Handoff: P14 — Bounded World Guide (Deterministic App Assistance)

## Summary of Accomplishments
- Implemented **P14 — Bounded World Guide (Deterministic App Assistance)**, strictly adhering to `docs/CONSTITUTION.md` (§2.3, §3.3, §7.2), `docs/CONTRACTS.md` (§2, §3.3, §7.2), `docs/packets/P14.md`, and deterministic domain invariants.
- Recorded architectural decision **DEC-013** in `docs/DECISIONS.md`: Deterministic Bounded In-App Knowledge Guide, Prompt-Injection Defenses, and Distinct Non-Artist Attribution.
- Implemented approved local knowledge cards in `src/data/guideKnowledge.ts`:
  - 8 approved cards covering membership, benefits, VieSHOP orders & fulfilment, session formats, moment capsules, customer support & reconciliation, avatar studio & asset lifecycle, and tenant portability.
  - Every card features verified destination routes (`actionLink`), source documentation attribution (`sourceTitle`), and last updated dates (`updatedAt`).
- Built deterministic query processor `queryWorldGuide(query)` in `src/data/guideKnowledge.ts`:
  - Prompt-injection and jailbreaking defense: Neutralizes attempts (`ignore previous instructions`, `grant me vip`, script tags, eval, update mutations) without modifying state (`injection_blocked`).
  - Honest limitation boundaries: Unsupported questions regarding artist private life, personal opinions, financial speculation, or medical advice decline with clear limitation notice (`unsupported_topic`).
  - Honest unknown query decline: Queries matching no approved topics return informative decline notice (`unknown`) with suggested topics, without guessing or hallucinating answers.
  - Keyword and token scoring delivering relevant approved knowledge cards with clickable navigation links.
- Built interactive `WorldGuidePanel` (`src/components/WorldGuidePanel.tsx`):
  - Prominent mandatory disclaimer banner: `Hướng dẫn demo · Không phải nghệ sĩ` (`guide-disclaimer-banner`).
  - Search input with submit button and quick-filter topic chips (`guide-topic-chip-*`).
  - Renders answer cards with source citations and deep links (`guide-answer-card`, `guide-action-link`).
  - Handles injection defense notices (`guide-injection-blocked-notice`), limitation notices (`guide-limitation-notice`), and unknown query notices (`guide-unknown-query-notice`).
- Mounted `WorldGuidePanel` inside `AppShell` (`src/components/AppShell.tsx`) with accessible launcher button (`guide-launcher-btn` / `open-world-guide-btn`) in sidebar and mobile navigation.
- Verified strict constitutional non-negotiables:
  - Zero outbound network calls (no `fetch`/`axios`), zero external API keys.
  - Zero live LLM calls (Real LLM integration strictly gated in packet X02).
  - Canned guide assistance is never deceptively labeled as live generative AI.
  - Guide is strictly read-only and cannot mutate state, update entitlements, or place orders.
- Verified Acceptance Test Suite `src/tests/guide.test.tsx` (13/13 passed):
  1. Answers membership navigation queries with valid link, source title, and updated date.
  2. Declines unknown queries honestly without inventing or hallucinating answers.
  3. Detects and blocks unsupported private, medical, financial, or artist-opinion topics.
  4. Neutralizes adversarial prompt-injection attempts and preserves system security.
  5. Opens guide panel from AppShell launcher button and displays mandatory disclaimer banner.
  6. Searches for membership and renders answer card with valid navigation link.
  7. Clicking quick topic chip renders corresponding approved knowledge card.
  8. Submitting prompt injection via UI displays injection blocked notice and mutates nothing.
  9. Submitting unsupported topic in UI renders limitation notice.
  10. Submitting unknown query in UI renders honest decline notice.
  11. Guarantees zero outbound network requests or external API keys.
  12. Ensures guide is strictly read-only and cannot mutate entitlements or place orders.
  13. Confirms canned behavior is not deceptively labeled as live generative AI.
- Total test suite: 159/159 passed across 15 test files.
- TypeScript typecheck passed cleanly (`tsc -b` exit code 0).
- Production build succeeded (`tsc -b && vite build` compiled in 4.08s).
- Updated `docs/TEST_MATRIX.md` marking T14 as `PASSED`.
- Updated `docs/BUILD_STATE.md` setting next packet to `P15 — External Tenant Portability (MFan & FanMe)`.

## Verified Evidence
- `npm.cmd run typecheck`: exit code 0 (passed at 2026-09-09T19:15:41+07:00)
- `npm.cmd test`: exit code 0, 159 passed across 15 test files (passed at 2026-09-09T19:15:08+07:00)
- `npm.cmd run build`: exit code 0, compiled successfully in 4.08s (passed at 2026-09-09T19:15:41+07:00)

## Next Resume Action
- Open `docs/packets/P15.md`.
- Implement **P15 — External Tenant Portability (MFan & FanMe)**:
  - Implement external tenant configurations and profile isolation for MFan and FanMe.
  - Verify data created in VieWorld does not leak into MFan or FanMe.
  - Verify switching back restores original state.
  - Verify roles and entitlements do not transfer across tenants.
  - Verify Acceptance T15 in `src/tests/portability.test.tsx`.
